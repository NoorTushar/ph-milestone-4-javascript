/**
 * Get max number from an array
 */

const numbers = [5, 7, 2, 4, 8, 1];

function getMax(arr) {
   let max = arr[0];

   for (number of numbers) {
      if (number > max) {
         max = number;
      }
   }

   return max;
}

const max = getMax(numbers);
console.log(max);
